name: Publish to WinGet

# Disabled for Orbit: no public release channel exists in Phase 5.
on: workflow_dispatch

permissions:
  contents: read

jobs:
  publish:
    # Deliberately disabled until an Orbit-specific package identity is reviewed.
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Orbit WinGet publication is disabled pending review"

name: Update gomod2nix.toml

# Disabled for Orbit until a reviewed, Orbit-owned dependency update workflow exists.
on: workflow_dispatch

permissions:
  contents: read

jobs:
  dependency-update:
    # Deliberately disabled: Phase 5 does not authorize automated dependency commits or pushes.
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Orbit dependency automation is disabled pending review"

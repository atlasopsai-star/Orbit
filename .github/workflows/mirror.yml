name: Mirror to Codeberg

# Disabled for Orbit: this inherited workflow targeted Superfile's Codeberg mirror.
on: workflow_dispatch

permissions:
  contents: read

jobs:
  mirror:
    # Deliberately disabled until Orbit has its own reviewed mirror target.
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Orbit mirroring is disabled pending review"
